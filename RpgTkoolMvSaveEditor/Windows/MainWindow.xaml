<Window x:Class="RpgTkoolMvSaveEditor.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:windows="clr-namespace:RpgTkoolMvSaveEditor.Windows"
        xmlns:controls="clr-namespace:RpgTkoolMvSaveEditor.Controls"
        d:DataContext="{d:DesignInstance Type=windows:MainWindowVM}"
        d:Width="800" d:Height="450"
        mc:Ignorable="d"
        Title="RPGTkoolSaveDataEditor" WindowState="Maximized" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="ファイル(_F)">
                <MenuItem Header="フォルダを開く(_O)" Command="{Binding OpenDirCmd}" />
            </MenuItem>
        </Menu>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <controls:CommonDataEditorControl Grid.Column="0"/>
        </Grid>
    </Grid>
</Window>