<UserControl x:Class="RpgTkoolMvSaveEditor.Controls.CommonDataEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:RpgTkoolMvSaveEditor.Controls"
             d:DataContext="{d:DesignInstance Type=controls:CommonDataControlVM}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <TabControl>
        <TabItem Header="Common">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <controls:GameSwitchesControl Grid.Column="0" Source="{Binding Switches}" />
                <controls:GameVariablesControl Grid.Column="1" Source="{Binding Variables}" />
            </Grid>
        </TabItem>
    </TabControl>
</UserControl>