<UserControl x:Class="RpgTkoolMvSaveEditor.Controls.SaveDataTabControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:RpgTkoolMvSaveEditor.Controls"
             d:DataContext="{d:DesignInstance Type=controls:SaveDataTabControlVM}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <TabControl ItemsSource="{Binding SaveDatas}">
        <TabControl.ItemTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding FileName}" />
            </DataTemplate>
        </TabControl.ItemTemplate>
        <TabControl.ContentTemplate>
            <DataTemplate>
                <controls:SaveDataControl Source="{Binding Data}" />
            </DataTemplate>
        </TabControl.ContentTemplate>
    </TabControl>
</UserControl>