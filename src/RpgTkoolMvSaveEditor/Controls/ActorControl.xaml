<UserControl x:Class="RpgTkoolMvSaveEditor.Controls.ActorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Name="root">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0">HP</Label>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding HP, ElementName=root}" />
        <Label Grid.Row="1" Grid.Column="0">MP</Label>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding MP, ElementName=root}" />
        <Label Grid.Row="2" Grid.Column="0">TP</Label>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding TP, ElementName=root}" />
        <Label Grid.Row="3" Grid.Column="0">Exp</Label>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Exp, ElementName=root}" />
    </Grid>
</UserControl>